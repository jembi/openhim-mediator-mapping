'use strict'

const tap = require('tap')

tap.test('Endpoint API Integration Tests', {autoend: true}, t => {
  t.test('should create new Endpoint', {autoend: true}, t => {
    t.plan(1)
    t.ok('TODO')
  })
})
